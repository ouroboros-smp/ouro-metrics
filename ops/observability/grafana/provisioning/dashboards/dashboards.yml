apiVersion: 1

providers:
  - name: ouroboros
    folder: Ouroboros
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
